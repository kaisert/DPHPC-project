

template<typename T_elem>
class Deflator {
    template<typename InputIterator, typename BackInserter>
    InputIterator deflate(RandIter begin, RandIter end, BackInserter target) {
    }

    int end();
}
